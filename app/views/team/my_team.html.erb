<h3>Your Team</h3>
<% if(@team != nil) then %>
   <div style="border: 1px solid lightgrey" class="post">
      <p>Team name: <%= @team.team_name %></p>
      <p>Team statement: <%= @team.team_statement %></p>
      <p>Leader: <%= @team.care_giver.firstname %> <%= @team.care_giver.lastname %></p>
      <% if(@members.length >= 2) %>
         <% @members.each do |member|%>
            <p>Member: <%= member.firstname %></p>
         <% end %>
      <% else %>
         <p>There are currently no other members</p>
      <% end %>
   </div>
   <p>user will also be able to see all members of the team and also leader</p>
<% else %>
   <p>You are currently not in a team</p>
<% end %>

<% if @families %>
   <p>You are not currently working with any families</p>
<% else %>
   <% @families.each do |family|%>
      <h4>working with </h4>
      <p><%=family.phone %></p>
      <p><%=family.recipient_relation %></p>
      <% if family.recipient %>
         <p><%= family.recipient.firstname %></p>
         <p><%= family.recipient.city %></p>
      <% end %>
   <% end %>
<% end %>