<div class="home-teamsearch-container">
   <div class="home-teamsearch-container__care-text">
      <h3  class="home-teamsearch-container__care-text--text">Do you want to find a Care team?</h3>
   </div>
   <%= form_tag search_team_results_path, :method => 'get' do %>
      <div class="teamsearch-name">
         <%= label :search, "Search for a team by the Teams Name" %>
         <div class="teamsearch-name__inputs">
            <%= text_field_tag :search, params[:search], placeholder: "Search Teams By name", required: true, class: "teamsearch-name__inputs--input" %>
            <%= submit_tag "Search", :name => nil, class: "teamsearch-name__inputs--button" %>
         </div>
      </div>
   <% end %>
   <p class="location-text">Or By Location</p>
   <%= form_tag search_team_results_state_path, :method => 'get' do %>
      <div class="teamsearch-name">
      <%= label :search, "State" %>
         <div class="teamsearch-name__inputs">
            <%= select_tag :search, options_for_select(CS.states(:us).map { |code, name| [name, code] }), {:id => "state-picker", :required => true, :prompt => "State", :class => "teamsearch-name__inputs--input" } %>
            <%= submit_tag "Search", :name => nil, class: "teamsearch-name__inputs--button"  %>
         </div>
      </div>
   <% end %>
   <%= form_tag search_team_results_city_path, :method => 'get' do %>
      <div class="teamsearch-name" style="margin-bottom: 4rem;">
         <%= label :search, "City" %>
         <div class="teamsearch-name__inputs">
            <%= text_field_tag :search, params[:search], placeholder: "Search Teams By city", required: true, class: "teamsearch-name__inputs--input" %>
            <%= submit_tag "Search", :name => nil, class: "teamsearch-name__inputs--button" %>
         </div>
      </div>
   <% end %>
</div>
<!--<p>Search through <%= @all_teams.length %> total teams</p> 
<% @teams.each do |team| %>
   <div style="border: 1px solid lightgrey" class="post">
      <p>team name:<%= team.team_name %></p>
      <p><%= team.team_city%>,<%= team.team_state %></p>
      <p><%= team.team_statement %></p>
      <%= link_to 'Contact this team', team_show_path(:id => team.id) %>
   </div>
<% end %>
<%= will_paginate @teams %>-->



